@extends('layouts.app') @section('content')<section class="product-detail section"><div class="container detail-grid"><div class="detail-image" style="--accent:{{ $product['accent'] ?? '#d7b66c' }}">@if(!empty($product['image']))<img src="{{ $product['image'] }}" alt="{{ $product['name']['tr'] }}" width="700" height="700">@else<div class="image-placeholder">nut<span>time</span></div>@endif</div><div class="detail-copy"><p class="eyebrow">{{ $product['category'] }}</p><h1>{{ $product['name']['tr'] }}</h1><p class="lead">{{ $product['description'] }}</p><div class="detail-rule"></div><p>Doğal içerikler, özenli üretim ve iyi hissettiren bir lezzet deneyimi.</p>@if(!empty($settings['whatsapp']))<a class="button" href="https://wa.me/{{ preg_replace('/\D/', '', $settings['whatsapp']) }}">WhatsApp ile bilgi al <span>↗</span></a>@else<a class="button" href="{{ url('/iletisim') }}">İletişime geç <span>↗</span></a>@endif</div></div></section>@if(count($related))<section class="section related"><div class="container"><p class="eyebrow">BUNLARI DA KEŞFEDİN</p><div class="product-grid">@foreach($related as $item)<x-product-card :product="$item" />@endforeach</div></div></section>@endif @endsection
