@extends('layouts.app')
@section('content')
<x-page-hero kicker="BİRLİKTE ÇALIŞALIM" title="Merhaba<br><em>diyelim.</em>" copy="Ürünlerimiz, iş birlikleri veya teklif talepleriniz için bize ulaşın." />
<section class="contact-page"><div class="container contact-page__grid"><aside><p class="kicker">İLETİŞİM</p><a href="mailto:{{ $settings['email'] ?? 'hello@nuttime.com.tr' }}">{{ $settings['email'] ?? 'hello@nuttime.com.tr' }}</a><a href="tel:{{ $settings['phone'] ?? '' }}">{{ $settings['phone'] ?? '+90 212 123 45 67' }}</a>@if(!empty($settings['address']))<p>{{ $settings['address'] }}</p>@endif</aside><form method="POST" action="{{ url('/iletisim') }}" class="contact-page__form">@csrf @if(session('success'))<div class="success">{{ session('success') }}</div>@endif<div class="honeypot"><label>Website<input name="website" tabindex="-1" autocomplete="off"></label></div><label>Ad soyad<input name="name" value="{{ old('name') }}" required></label>@error('name')<small>{{ $message }}</small>@enderror<label>E-posta<input type="email" name="email" value="{{ old('email') }}" required></label>@error('email')<small>{{ $message }}</small>@enderror<label>Mesajınız<textarea name="message" rows="6" required>{{ old('message') }}</textarea></label>@error('message')<small>{{ $message }}</small>@enderror<button class="button" type="submit">Mesajı gönder <span>↗</span></button></form></div></section><x-factory-location :factory="$factory" />
@endsection
