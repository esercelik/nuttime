@extends('layouts.app')
@section('content')
<section class="category-intro">@if($category['image'])<img src="{{ $category['image'] }}" alt="{{ $category['image_alt'] ?: $category['name'] }}" width="1800" height="900" fetchpriority="high">@endif<div class="category-intro__shade"></div><div class="container category-intro__copy"><p class="kicker">{{ __('site.catalog.category_count', ['count' => str_pad((string) count($products), 2, '0', STR_PAD_LEFT)]) }}</p><h1><x-safe-rich-text :value="$category['name']" /></h1>@if($category['description'])<p><x-safe-rich-text :value="$category['description']" /></p>@endif</div></section><section class="catalog-page catalog-page--editorial"><div class="container"><div class="catalog-page__bar"><a href="{{ app(\App\Support\LocalizedUrl::class)->route('products') }}">← {{ __('site.actions.all_products') }}</a></div><div class="catalog-page__grid">@forelse($products as $index => $product)<x-product-card :product="$product" :index="$index" :priority="$index < 2" :variant="$index === 0 ? 'hero' : 'default'" />@empty<p class="empty-state">{{ __('site.catalog.category_empty') }}</p>@endforelse</div></div></section>
@endsection
