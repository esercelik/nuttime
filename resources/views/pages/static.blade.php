@extends('layouts.app') @section('content')<section class="page-hero"><div class="container"><p class="eyebrow">{{ $page === 'about' ? 'NUTTIME HAKKINDA' : 'GÜVENLE ÜRETİYORUZ' }}</p><h1>{{ $page === 'about' ? 'Hakkımızda' : 'Sertifikalarımız' }}</h1></div></section>@if($page === 'certificates')<section class="section"><div class="container"><div class="certificate-grid">@forelse($certificates as $certificate)<article class="certificate-card">@if($certificate['image'])<img src="{{ $certificate['image'] }}" alt="{{ $certificate['name'] }}" width="320" height="220">@else<div class="certificate-mark">nt</div>@endif<div><p>{{ $certificate['name'] }}</p><small>{{ $certificate['description'] }}</small></div>@if($certificate['document'])<a href="{{ $certificate['document'] }}" target="_blank" rel="noopener">Belge ↗</a>@endif</article>@empty<p>Kalite belgelerimiz yakında burada yer alacak.</p>@endforelse</div></div></section>@else<section class="section editorial"><div class="container narrow"><h2>Doğadan gelen iyi fikri,<br><em>hayata taşıyoruz.</em></h2><p>Nuttime, doğanın sunduğu en iyi lezzetleri çağdaş ve yalın bir deneyime dönüştürmek için kuruldu. Ürünlerimizi seçerken kaynağa, üretirken özen ve şeffaflığa inanıyoruz.</p><a class="button" href="{{ url('/iletisim') }}">Bize ulaşın <span>↗</span></a></div></section>@endif @endsection
